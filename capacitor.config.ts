import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.commpansers.pathshala',
  appName: 'Commanders Pathshala',
  webDir: 'dist'
};

export default config;
